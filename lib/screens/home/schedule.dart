import 'package:flutter/material.dart';
import 'schedule-day-card.dart';

class Schedule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: DayCard(),
    );
  }
}
